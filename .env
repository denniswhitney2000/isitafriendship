# Top level data directory
DATA_DIR = 'data'

# Number of batchs for processing
BATCH_SIZE = 32

# Epocs for model training
NUM_EPOCHS = 10

# Define the location of the saved model
MODEL_PATH = 'saved_model/friendship_sloop_detector.keras'

# Define the location of the images to be classified during testng
IMAGE_PATH = 'images/prediction'